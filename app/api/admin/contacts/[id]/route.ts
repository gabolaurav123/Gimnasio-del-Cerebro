import { z } from "zod";
import { requestIsAdmin } from "../../../../../lib/auth";
import { getContact, updateContact } from "../../../../../db/repository";
const schema = z.object({ status: z.enum(["NEW", "CONTACTED", "INTERESTED", "FOLLOW_UP", "ENROLLED", "NOT_INTERESTED", "CLOSED"]), nextFollowUp: z.string().max(30).optional(), note: z.string().max(1200).optional() });
export async function GET(request: Request, context: { params: Promise<{ id: string }> }) { if (!(await requestIsAdmin(request, ["SUPERADMIN", "COMERCIAL"]))) return Response.json({ error: "No autorizado" }, { status: 401 }); return Response.json(await getContact((await context.params).id)); }
export async function PATCH(request: Request, context: { params: Promise<{ id: string }> }) { if (!(await requestIsAdmin(request, ["SUPERADMIN", "COMERCIAL"]))) return Response.json({ error: "No autorizado" }, { status: 401 }); const parsed = schema.safeParse(await request.json()); if (!parsed.success) return Response.json({ error: "Datos inválidos" }, { status: 400 }); await updateContact((await context.params).id, parsed.data.status, parsed.data.nextFollowUp || null, parsed.data.note); return Response.json({ ok: true }); }
