import { z } from "zod";
import { requestIsAdmin } from "../../../../lib/auth";
import { createPost, getPosts } from "../../../../db/repository";
const image = z.string().trim().max(500).refine((value) => !value || /^\/api\/media\/[a-z0-9-]+$/i.test(value), "Imagen inválida");
const schema = z.object({ title: z.string().trim().min(5).max(220), slug: z.string().regex(/^[a-z0-9-]+$/), excerpt: z.string().trim().min(10).max(500), content: z.string().trim().min(10).max(30000), category: z.string().trim().min(2).max(80), author: z.string().trim().max(120).optional().default(""), image: image.optional().default(""), attachmentUrl: image.optional().default("") });
export async function GET(request: Request) { if (!(await requestIsAdmin(request, ["SUPERADMIN", "EDITOR"]))) return Response.json({ error: "No autorizado" }, { status: 401 }); return Response.json({ posts: await getPosts(true) }); }
export async function POST(request: Request) { if (!(await requestIsAdmin(request, ["SUPERADMIN", "EDITOR"]))) return Response.json({ error: "No autorizado" }, { status: 401 }); const parsed = schema.safeParse(await request.json()); if (!parsed.success) return Response.json({ error: "Datos inválidos", details: parsed.error.flatten() }, { status: 400 }); return Response.json({ id: await createPost({ ...parsed.data, image: parsed.data.image || null, attachmentUrl: parsed.data.attachmentUrl || null, author: parsed.data.author || null }) }, { status: 201 }); }
