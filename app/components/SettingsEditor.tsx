"use client";
import { Check, Save } from "lucide-react";
import { FormEvent, useState } from "react";

const fieldLabels: Record<string, string> = { siteName: "Nombre del sitio", whatsapp: "Número de WhatsApp", contactEmail: "Email de contacto", heroEyebrow: "Eyebrow del hero", heroTitle: "Título principal", heroDescription: "Descripción del hero", ctaTitle: "Título del CTA final", ctaDescription: "Descripción del CTA final", instagram: "Instagram", facebook: "Facebook", youtube: "YouTube" };
export function SettingsEditor({ settings, fields }: { settings: Record<string, string>; fields: string[] }) { const [saved, setSaved] = useState(false); async function submit(event: FormEvent<HTMLFormElement>) { event.preventDefault(); const response = await fetch("/api/admin/settings", { method: "PATCH", headers: { "content-type": "application/json" }, body: JSON.stringify(Object.fromEntries(new FormData(event.currentTarget))) }); setSaved(response.ok); } return <form className="settings-form" onSubmit={submit}>{fields.map((field) => <label key={field}>{fieldLabels[field] ?? field}{field.toLowerCase().includes("description") ? <textarea name={field} defaultValue={settings[field]} rows={4} /> : <input name={field} defaultValue={settings[field]} />}</label>)}<button className="button button--primary"><Save size={17} />Guardar cambios</button>{saved && <span className="inline-success"><Check size={16} />Configuración actualizada</span>}</form>; }
