import { ArrowLeft, CalendarClock, Mail, MapPin, MessageCircle, Phone } from "lucide-react";
import { notFound } from "next/navigation";
import { getContact } from "../../../../db/repository";
import { requireAdminRole } from "../../../../lib/admin-access";
import { ContactEditor, StatusBadge } from "../../../components/AdminUI";

export default async function ContactDetail({ params }: { params: Promise<{ id: string }> }) { await requireAdminRole(["SUPERADMIN", "COMERCIAL"]); const { id } = await params; const data = await getContact(id); if (!data) notFound(); const { contact, notes, activities } = data; const phone = contact.phone.replace(/\D/g, ""); return <><a className="admin-back" href="/admin/crm"><ArrowLeft size={16} />Volver al CRM</a><div className="admin-page-heading"><div><span>Ficha de contacto</span><h1>{contact.name}</h1><p>Creado el {new Intl.DateTimeFormat("es-AR", { dateStyle: "long" }).format(new Date(contact.createdAt))}</p></div><StatusBadge status={contact.status} /></div><div className="contact-detail-grid"><section className="admin-card"><div className="admin-card__heading"><h2>Información</h2></div><dl className="contact-data"><div><Mail /><dt>Email</dt><dd><a href={`mailto:${contact.email}`}>{contact.email}</a></dd></div><div><Phone /><dt>WhatsApp</dt><dd>{contact.phone}</dd></div><div><MapPin /><dt>País</dt><dd>{contact.country}</dd></div><div><CalendarClock /><dt>Interés</dt><dd>{contact.trainingInterest || "Por definir"}</dd></div></dl><a className="button button--whatsapp" href={`https://wa.me/${phone}`} target="_blank" rel="noreferrer"><MessageCircle size={18} />Abrir WhatsApp</a></section><section className="admin-card"><div className="admin-card__heading"><h2>Estado comercial</h2></div><ContactEditor contact={contact} /></section><section className="admin-card admin-card--wide contact-message"><div className="admin-card__heading"><div><h2>Consulta recibida</h2><p>Mensaje enviado desde el formulario público.</p></div></div><blockquote>{contact.message}</blockquote></section><section className="admin-card admin-card--wide"><div className="admin-card__heading"><div><h2>Historial</h2><p>Notas y cambios relevantes.</p></div></div>{[...notes, ...activities].length ? <div className="timeline">{[...notes, ...activities].map((item) => <div key={String(item.id)}><span /><p><strong>{String(item.body ?? item.action ?? "Actividad")}</strong><time>{new Intl.DateTimeFormat("es-AR", { dateStyle: "medium", timeStyle: "short" }).format(new Date(String(item.created_at)))}</time></p></div>)}</div> : <div className="admin-empty"><CalendarClock /><h3>Sin actividad adicional.</h3><p>Las notas y cambios aparecerán aquí.</p></div>}</section></div></>; }
